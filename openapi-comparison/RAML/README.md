# RAML Demo

## Render documentation
You can use RAML-compatible tools such as API Console or RAML-to-HTML.

## Notes
RAML is good for structured, reusable API design and supports reusable types well.

## Optional test workflow
RAML is mainly design-focused. In practice, many teams convert or mirror RAML definitions into OpenAPI for broader tooling support.